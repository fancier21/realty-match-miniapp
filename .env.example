# Leave empty for local development; Vite proxies /api to localhost:8000.
# Production should point to the deployed backend API (e.g. https://api.yourdomain.com).
VITE_API_BASE_URL=https://api.yourdomain.com
